var typed = new Typed(".text", {
    strings: ["Frontend Developer", "Software Developer", "Web Developer"], // The strings that will be typed
    typeSpeed: 100,  // Speed at which the text is typed (in milliseconds)
    backSpeed: 100,  // Speed at which the text is deleted (in milliseconds)
    backDelay: 1000, // Delay before starting to delete (in milliseconds)
    loop: true       // Whether to loop the animation or not
});

